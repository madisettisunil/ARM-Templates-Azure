*******Following command will deploy Empty resource group in Azure subscription*************

az deployment create \
  --subscription defense.fun \
  --name demoDeployment \
  --location westeurope \
  --template-uri https://raw.githubusercontent.com/madisettisunil/ARM-Templates-Azure/master/emptyRG.json \
  --parameters rgName=demoResourcegroup rgLocation=westeurope
