*******Following command will deploy Empty resource group in current Azure subscription*************

az deployment create \
  --name demoDeployment \
  --location westeurope \
  --template-uri https://raw.githubusercontent.com/madisettisunil/ARM-Templates-Azure/master/emptyRG.json \
  --parameters rgName=demoResourcegroup rgLocation=westeurope

New-AzDeployment `
  -Name demoDeployment `
  -Location centralus `
  -TemplateUri https://raw.githubusercontent.com/Azure/azure-docs-json-samples/master/azure-resource-manager/emptyRG.json `
  -rgName demoResourceGroup `
  -rgLocation westeurope


*******Following command will deploy Empty Workspace in current Azure subscription*************

New-AzResourceGroupDeployment -ResourceGroupName demoResourcegroup -TemplateFile ./template.json -workspacename blabla -location westeurope

*******Following command will deploy lighthouse connector to resource group*************

az deployment create –-name Lighthouse \
                     –-location westeurope \
                     --template-uri https://raw.githubusercontent.com/madisettisunil/ARM-Templates-Azure/master/rgDelegatedResourceManagement.json \
                     --parameters RGdelegatedResourceManagement.parametres.json \
                     --verbose
